# 交易评分、港股折算与组合指标修复计划

## Summary

本轮只修复以下五项：交易评分时间穿越、港股币种折算、组合指标口径、前瞻 PB、缓存生命周期。组合估值只保留打包后的综合 PE/PB 及其历史分位，不再计算持仓个股分位均值。现有 API 路径保持兼容，新增字段采用向后兼容方式；SQLite 自动迁移前先备份数据库。

## Implementation Changes

### 1. 冻结交易评分，消除时间穿越

- 新增 `trade_score_snapshots`，每笔交易保存因子原值、因子数据日期、权重、模型版本、覆盖率、评分和快照状态。
- 当前交易使用成交价计算涨跌和实时估值；PE/PB 分位只使用交易日及以前的数据，集中度使用交易发生前的持仓。
- 新增评分状态：
  - `frozen`：交易发生时生成的正式快照。
  - `estimated`：旧交易根据本地历史缓存回填。
  - `insufficient`：有效因子权重低于 60%，不评分、不评级。
- 缺失因子不再简单放大其他因子。评分采用 `50 + (已知因子得分 - 50) × coverage`；覆盖率 60%～80% 标记低置信度，80% 以上正常评级。
- 日评分只聚合交易快照，按人民币成交金额加权；若可评分成交金额不足当日总金额的 80%，日评分不评级。
- 刷新行情、财务或修改评分权重不再改变历史快照；权重修改只作用于之后的交易，并生成新的模型版本。
- 修改交易时重新生成该交易快照；删除交易时删除对应快照并重算日聚合。
- 现有交易使用本地交易日行情、历史估值和交易前持仓回填；无法确认当时可见的财务数据不使用，统一标记 `estimated`。

### 2. 港股统一折算为人民币

- 股票增加 `currency`，五位港股代码迁移为 `HKD`，其余为 `CNY`；港股不再复用 ETF 分类。
- 新增 `fx_rate_cache`，保存日期、币种对、人民币汇率、来源和更新时间。
- 汇率采用中行港币折算价，单位转换为 `1 HKD = x CNY`；缺失时依次使用央行中间价、买卖价中点。非交易日使用最近一个有效汇率。
- 动态和全量刷新在存在港股时自动刷新 HKD/CNY；历史回填覆盖最早港股交易日至今。
- 交易保留原币价格和金额，并新增交易日 `fx_rate`、`amount_cny`；持仓同时维护原币平均成本和人民币平均成本。
- 组合市值使用当前汇率，成本使用每笔买入时的历史汇率，因此港股盈亏包含汇率损益。
- 汇率缺失时绝不按 1:1 计算：该港股显示原币数据，人民币汇总中暂时剔除并返回明确的 `missing_fx`。
- 波动率使用“复权港股价格 × 当日汇率”的人民币收益率序列。

### 3. 统一组合指标口径

- 所有总市值、总成本、权重和盈亏统一以 CNY 计算，股票、ETF、港股全部参与资产和风险指标。
- ETF 不再按类型强制排除；基本面指标按“是否有对应数据”决定是否参与，并返回市值覆盖率。
- 使用穿透式指标：
  - 持仓归属利润 = 持股数 ÷ 总股本 × 公司 TTM 利润。
  - 持仓归属净资产 = 持股数 ÷ 总股本 × 公司净资产。
  - 综合 PE = 有效持仓市值 ÷ 归属利润合计。
  - 综合 PB = 有效持仓市值 ÷ 归属净资产合计。
  - 综合 ROE = 归属利润合计 ÷ 归属净资产合计。
  - 综合增长率使用同一覆盖股票集合的两期归属利润或营收合计计算。
- 亏损公司保留负利润贡献；组合归属利润小于零时综合 PE 正常显示负数，只有组合归属利润等于零时综合 PE 返回不适用。
- 组合归属净资产小于零时综合 PB 正常显示负数，只有组合归属净资产等于零时综合 PB 返回不适用。
- PE、PB、ROE 必须使用完全相同的股票集合、财务口径、币种和报告期，并满足 `综合ROE = 综合PB ÷ 综合PE`；百分比展示时乘以 100。
- 每个指标返回 `coverage_weight` 和剔除原因，低于 70% 时前端显示“覆盖不足”，不进入组合评分。
- 首页 `pe_pct/pb_pct` 只使用打包后的组合历史序列分位；删除持仓个股分位均值、`weighted_stock_pe_pct` 和 `weighted_stock_pb_pct`。个股详情和持仓明细仍保留每只股票自己的分位。
- 组合历史序列明确命名为“当前持仓篮子历史估值”，继续使用当前篮子观察历史估值区间，不冒充真实历史持仓。
- 历史组合 PE/PB 使用当前组合的人民币市值固定权重打包计算；历史单日估值覆盖当前组合市值不足 90% 时，该日不进入分位样本。
- 历史数据统一升序，只保留真实交易日；分位样本只取当前估值日期之前的有效值，移除依赖 `[:-1]` 的错误剔除方式。有效样本不足 60 个交易日时返回样本不足。
- 正负 PE 使用分段排序形成统一的 0～100 分位，排序从“较便宜/较好”到“较贵/较差”：正 PE 从小到大 → 利润为零 → 负 PE 从绝对值大到绝对值小。例如 `8, 15, 30, 0, -100, -20, -5`。
- 正负 PB 使用同样的分段排序：正 PB 从小到大 → 净资产为零 → 负 PB 从绝对值大到绝对值小。
- 利润/净资产为零与数据缺失必须使用独立状态字段区分；零值可参与分位排序，但对应 PE/PB 数值显示“不适用”，缺失数据不参与分位。
- 波动率纳入股票、ETF 和港股，使用人民币复权价格；不同市场休市日最多前向填充 3 个自然日，日覆盖率不足 95% 的日期不纳入样本。

### 4. 修正前瞻 PB

- 财务缓存补充上一年年末归母净资产，避免将全年预测利润重复加到当前季度净资产。
- 使用：
  - 预测净利润 = 上年净利润 × `(1 + 预期利润增速)`。
  - 预测分红 = 预测净利润 × 上年支付率。
  - 预测年末净资产 = 上年年末净资产 + 预测净利润 − 预测分红。
  - 前瞻 PB = 当前市值 ÷ 预测年末净资产。
- 前瞻 ROE 同步改为预测净利润除以上年末和预测年末平均净资产。
- 普通股票的前瞻 PB 必须尽可能计算并展示，不能因为普通缓存缺失直接返回空值。数据缺失时先触发显式财务刷新，再按以下降级链路补全：
  - 预期利润增速：用户输入 → 最新 TTM 增长 → 最新财报同比 → 0% 保守假设。
  - 预期支付率：用户输入 → 上年支付率 → 最近可用历史支付率 → 0% 保守假设。
  - 上年末净资产：上年年报归母净资产 → 上年末每股净资产 × 总股本。
- 上年末净资产仍无法取得时，使用次级推演：`预测年末净资产 = 最新净资产 + (预测全年净利润 − 当前已报告累计净利润) × (1 − 预期支付率)`。
- 前瞻 PB 返回 `high/medium/low/invalid` 可信度和全部计算假设。使用完整年报数据为 `high`，使用最新净资产推演为 `medium`，使用零增长或零支付率等保守假设为 `low`。
- 只有 ETF 等指标本身不适用、股价/股本完全缺失或预测净资产等于零时，前瞻 PB 才显示“不适用”；预测净资产为负时前瞻 PB 正常显示负数并标记 `invalid`，同时返回 `fwd_pb_reason`。
- 组合前瞻 PB 使用各持仓预测净资产穿透汇总，不再对个股前瞻 PB 做简单加权。

### 5. 修复缓存生命周期

- 清仓只修改持仓状态，不再删除日 K、财务、估值历史、分位和汇率等原始缓存。
- 再次开仓直接复用历史缓存；刷新逻辑根据更新时间补增量，不再依赖“是否首笔交易”判断。
- 将缓存分为：
  - 原始缓存：行情、财务、估值历史、汇率，长期保留。
  - 不可变快照：交易评分快照。
  - 派生缓存：组合历史序列和组合分位，可失效重建。
- 组合派生缓存增加 `portfolio_hash`，由持仓数量、币种、价格版本和估值版本生成；交易变更或刷新成功后重建。
- GET 接口只读缓存；缓存缺失时返回 HTTP 409 和结构化 `CACHE_MISS`，不再在个股详情或组合读取过程中联网、写库或懒重建。
- 用户在搜索框选择股票并点击确定后，前端先读取缓存状态；如缺少行情、财务或估值历史，弹窗列出缺失项并询问是否下载。
- 用户选择“下载并打开”后，由前端调用 `POST /api/stocks/{code}/refresh/full`，下载成功后自动重试 GET 并打开个股页；选择“仅打开已有数据”时展示部分数据和缺失标记；选择“取消”时不发起下载。
- 下载失败时保留搜索结果并展示具体失败项，禁止自动无限重试。
- 保留显式数据重置功能，并将新汇率、评分快照和迁移表纳入重置范围。

## API and Migration

- 保留现有路由，主要新增：
  - 股票/持仓：`currency`、`fx_rate`、`value_native`、`value_cny`、`cost_cny`。
  - 交易：`amount_cny`、`score_snapshot_status`、`score_coverage`、`model_version`。
  - 日评分：`coverage`、`status`、`model_version`、`estimated_count`。
  - 组合指标：打包后的 `pe/pb/roe`、`pe_pct/pb_pct`、各指标的 `coverage_weight`、正负/零/缺失状态和缺失原因；不再返回个股分位均值字段。
  - 刷新响应：`fx` 刷新结果；刷新选项增加 `fx`。
- 个股缓存缺失响应统一为 HTTP 409：`{"code":"CACHE_MISS","stock":"...","missing_items":[],"available_items":[],"can_refresh":true}`。
- 前瞻 PB 新增 `fwd_pb_confidence`、`fwd_pb_reason` 和用于计算的增速、支付率、净资产来源字段。
- `/api/scoring/rebuild` 改为只重建日聚合，不重新计算冻结快照。
- 新增事务化数据库版本迁移；迁移前生成时间戳备份。
- 迁移现有数据时：
  - 推断并写入币种。
  - 使旧港股人民币金额等待汇率刷新后回填，期间不按 1:1 计算。
  - 本地回填旧交易评分快照并标记 `estimated/insufficient`。
  - 清空并重建旧组合派生缓存和日评分聚合。

## Test Plan and Acceptance Criteria

- 刷新行情、刷新财务和修改评分权重后，已有交易快照及历史日评分不变化。
- 回填历史交易时不读取交易日之后的行情、估值或财务；单一 10% 因子不能产生正式 A 级评分。
- 日评分按 `amount_cny` 加权，并正确处理部分交易评分不足。
- 港股市值按当前汇率折算、成本按交易日汇率折算；缺汇率时不进入人民币汇总。
- 当前持仓中的 `06198` 不再被识别为 ETF，组合权重、盈亏和波动率均按人民币计算。
- 首页 PE/PB 分位与 1 年打包组合历史序列分位完全一致；API 和前端不存在持仓个股分位均值。
- 亏损股对组合利润产生负贡献；组合总利润为负时 PE 显示负数，仅利润为零时显示不适用。
- 正负 PE/PB 的历史样本按分段规则排序；直接按普通数值升序计算负数分位的实现必须被测试拒绝。
- ROE 和增长率按穿透合计公式验证，不再使用市值加权平均；同一覆盖集合下必须满足 `ROE% = PB ÷ PE × 100`。
- ETF 和港股进入组合波动率；缺数据日期遵守 95% 覆盖门槛。
- 普通股票在完整数据、次级推演和保守假设三种路径下均能输出前瞻 PB；预测净资产为负时输出负 PB 和 `invalid` 标记，只有净资产为零或指标本身不适用时显示不适用。
- 清仓前后的原始缓存行数不变；再次开仓无需重新下载完整历史。
- 所有 GET 请求在测试中断言无网络调用、无数据库写入；首次选择未缓存股票时必须出现下载确认弹窗，确认后执行 POST 刷新并自动重试 GET，取消时不产生写操作。
- 迁移测试使用现有数据库副本，确认交易、持仓和用户预期增速不丢失。

## Assumptions

- 组合基础币种固定为 CNY。
- 港股汇率采用中行港币人民币折算价，不使用固定汇率。
- 评分最低有效因子覆盖率为 60%，日成交金额评分覆盖率门槛为 80%。
- 当前持仓篮子的历史估值用于判断当前组合贵贱，不作为真实历史持仓回测。
- 本轮不扩展已实现盈亏、分红现金账本、安全认证和前端 XSS 修复。
